
    private boolean isAppetiteRetrieved(List<RiskPosition> riskPositions) {
        if (riskPosition != null && !riskPositions.isEmpty() && !isOnlyAddedCodes(riskPositions)) {
//TODO: THIS IS NEVER TRUE, where is the bug?
            return true;
        }
        return false;
    }


int main()
{
    struct tm * tmp;
    time_t s;

    for (; ;) {
        s = time(NULL);
        tmp = localtime(&t);

        printf("%d:%0d:%d\n", temp ->tm_hour, tmp ->tm_min, tmp ->tm_sec);
//TODO: always get an error. Where is the bug? 
        Sleep(1000);

        system("cls");

    }

    return 0;
}


